apiVersion: v1
kind: Service
metadata:
  name: foodordering-service
  labels:
    app: foodordering
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: foodordering
